node_modules/
out/
release/
.shots/
*.log
release/
resources/js-debug/

# Incremental compiler state: a build artifact, and noise in every diff.
*.tsbuildinfo

# Claude Code settings for this machine only: its own allow-list, never shared.
.claude/settings.local.json
